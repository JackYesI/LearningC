// Home.cpp : This file contains the 'main' function. Program execution begins and ends there.
//

#include <iostream>
using std::cout;
using std::endl;
using std::cin;

int main()
{
    // First

    /*int hour, minute, second, times;

    cout << "enter seconds: ";
    cin >> times;
    hour = times / 3600;
    minute = (times - hour * 3600) / 60;
    second = times - hour * 3600 - minute * 60;
    cout << "hour :: " << hour << " minute: " << minute << " seconds: " << second;
    return 0;*/

    // Second

    /*double number;

    cout << "enter number: ";
    cin >> number;
    cout << "UAH - " << (int)number;
    cout << " coin - " << (number - (int)number) * 100;
    return 0;*/

    // Thirth

    /*double distance, times, hour;
    int min, sec;
    cout << "Calculation of running speed.\n";
    cout << "Enter the length of the distance: ";
    cin >> distance;
    cout << "Enter time min.sec: ";
    cin >> times;
    cout << "Distance :: " << distance << " m.\n";
    distance = distance / 1000;
    min = int(times);
    sec = min * 60 + (times - min) * 100;
    cout << "Time: " << min << " minute " << (times - min) * 100 << " second = " << sec << " seconds" << endl;
    hour = double(double(sec) / 3600);
    cout << "You run with speed --> " << distance / hour << " km / hour.";*/

    // Fourth

    /*int day, week;

    cout << "enter day: ";
    cin >> day;
    week = day / 7;
    cout << "weeks :: " << week;
    cout << " day :: " << day - week * 7;
    return 0;*/
}

// Run program: Ctrl + F5 or Debug > Start Without Debugging menu
// Debug program: F5 or Debug > Start Debugging menu

// Tips for Getting Started: 
//   1. Use the Solution Explorer window to add/manage files
//   2. Use the Team Explorer window to connect to source control
//   3. Use the Output window to see build output and other messages
//   4. Use the Error List window to view errors
//   5. Go to Project > Add New Item to create new code files, or Project > Add Existing Item to add existing code files to the project
//   6. In the future, to open this project again, go to File > Open > Project and select the .sln file
